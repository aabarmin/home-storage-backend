spring:
  datasource:
    url: jdbc:h2:mem:./db/testdb
    username: sa
    password:

storage:
  region: eu-west-2
  valid-seconds: 30
  bucket: abarmin-home-storage-dev-2022

management:
  endpoints:
    web:
      cors:
        allowed-methods: GET
        allowed-origins: '*'
      exposure:
        include: info,health