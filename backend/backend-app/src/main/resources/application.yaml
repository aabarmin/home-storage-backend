spring:
  datasource:
    url: jdbc:h2:file:./storage/database-dev.db
  liquibase:
    change-log: classpath:/db/db.changelog-master.xml

storage:
  local:
    folder: ./storage

management:
  endpoints:
    web:
      cors:
        allowed-methods: GET
        allowed-origins: '*'
      exposure:
        include: info,health