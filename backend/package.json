{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for the Home IS",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "start": "node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch ./src -e ts --exec run-s build start"
  },
  "author": "Aleksandr Barmin",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.3"
  }
}
