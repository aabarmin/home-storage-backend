/**
 * Consumption type. 
 */
export enum ConsumptionType {
    /**
     * The unit like kg or pack is consumed at once. 
     */
    UNIT_CONSUMPTION, 
    /**
     * The unit is consumed partially like 0.5 kg. 
     */
    PARTIAL_CONSUMPTION
}